# ============================================================
# vibecleaner - packaged backend runtime dependencies
# ============================================================
# Use this file when building the Tauri backend sidecar.
# It intentionally excludes optional Torch runtimes and local model files.
# Models are downloaded on demand into the user's app data directory.
# ============================================================

# --- Web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-multipart>=0.0.9

# --- Image processing ---
numpy>=1.24.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# --- GUI/text rendering bridge ---
PySide6>=6.6.0

# --- ONNX runtime (default detection, OCR, inpainting) ---
onnxruntime>=1.17.0

# --- HTTP client and translation providers ---
requests>=2.31.0
deep-translator>=1.11.0

# --- Japanese text normalization ---
jaconv>=0.3.0
regex>=2024.5.15

# --- OCR post-processing ---
pyclipper>=1.3.0
shapely>=2.0.0
