# ============================================================
# vibecleaner - Python backend dependencies
# ============================================================
# Full development environment dependencies.
# For release sidecar packaging, install requirements-runtime.txt instead.
# Optional Torch dependencies are also mirrored in requirements-torch.txt.
# ============================================================

# --- Web framework ---
# (Removed: fastapi, uvicorn, pydantic, python-multipart - now a CLI app)

# --- Image processing ---
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# --- GUI (text rendering) ---
PySide6>=6.6.0

# --- ONNX runtime (detection, OCR, inpainting) ---
onnxruntime>=1.17.0

# --- HTTP client (translation providers) ---
requests>=2.31.0

# --- Japanese text normalization ---
jaconv>=0.3.0

# --- OCR post-processing ---
pyclipper>=1.3.0
shapely>=2.0.0

# --- Translation providers ---
deep-translator>=1.11.0

# --- Optional: PyTorch-based models (inpainting, Manga OCR, RT-DETR, font detection) ---
torch>=2.0.0
torchvision>=0.15.0
torchmetrics>=0.11.0
pytorch-lightning>=2.0.0
